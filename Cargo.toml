[package]
name = "strix-pipeline"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "strix-pipeline"
path = "src/strix-pipeline.rs"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
